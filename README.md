# claude-code-jobs

Daily job scraper for Customer Success and Operations roles in Israel.

- `seen_jobs.json` — IDs of jobs already reported (used for deduplication).
- `latest_results.json` — results from the most recent scan.
